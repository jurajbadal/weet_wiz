{
  "name": "weetwiz-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute weetwiz-db --local --file=schema.sql",
    "db:init:remote": "wrangler d1 execute weetwiz-db --remote --file=schema.sql"
  },
  "dependencies": {
    "resend": "^4.0.0",
    "stripe": "^16.0.0"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
